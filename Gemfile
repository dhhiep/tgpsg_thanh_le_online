source 'https://rubygems.org'
git_source(:github) { |repo| "git@github.com:#{repo}.git" }

gem 'aws-sdk-s3', '~> 1.84'
gem 'dotenv', '~> 2.7'
gem 'httparty', '~> 0.16'

group :development do
  gem 'rake', '~> 13.0'
end

group :test do
  gem 'minitest'
  gem 'pry'
end
